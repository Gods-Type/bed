# bed
im trying to make an extention if i really can't then i'm gna do #ZED_CLONE

add a toggle beside the Projet Panel button on the bottom of the editor and it is to be called Tab Panel
add a action pane: toggle Tab Panel
add a action pane: reveal in Tab Panel
Ctrl-Shift-T to pane: reveal in Tab Panel
Ctrl-Shift-T to pane: toggle Tab Panel
Tab Panel this has the Context of TabPanel

When you open up the Tab Panel it will be identical to the Outline panel and the Project panel in both functionality and visuals
Search Funtionality

Update on Zed are pull to this repo though github action workflows so what i do in this repo does not affect their repo

# ZED_CLONE
when this repo updates the main branch, ones that downloaded before the update should get updates as well (just like Zed)
https://developer.chrome.com/docs/webview/get-started
